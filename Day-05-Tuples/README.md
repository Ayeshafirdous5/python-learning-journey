## Part 1 - Tuples

Learned the basics of tuples and how they are used to store multiple values in a single variable.

### Topics Practiced

- Creating tuples
- Accessing tuple items using indexes
- Zero-based indexing
- Negative indexing
- Tuple slicing
- Tuple methods
- `count()` - Counting occurrences of a value
- `index()` - Finding the position of a value
- Tuple immutability
- Using tuples to store structured data
- Accessing individual values from a tuple

### Practice

- Created tuples containing different types of data
- Accessed values using positive and negative indexes
- Extracted portions of tuples using slicing
- Counted repeated values using `count()`
- Found the position of values using `index()`
- Understood why tuple items cannot be modified
- Used tuples to store employee information
- Accessed employee name, role, salary, and location from a tuple

## Part 2 - Tuple Unpacking

Learned how to unpack tuple values into separate variables and work with multiple values efficiently.

### Topics Practiced

- Tuple unpacking
- Unpacking employee data
- Assigning tuple values to variables
- Swapping values using tuple unpacking
- Unpacking with `*`
- Extracting first, middle, and last values
- Working with real-world sales data using tuples

### Practice

- Unpacked employee information into separate variables
- Swapped two values without using a temporary variable
- Used `*` to collect remaining tuple values
- Extracted first, middle, and last values from a tuple
- Unpacked sales records into separate variables
- Used tuple unpacking with practical examples

More updates will be added as I continue learning Python.